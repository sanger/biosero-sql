-- To drop and recreate the database
-- NB. set the correct database name for the environment
-- NB. Use CAUTION, this drops the existing database and so will lose all data.
DROP DATABASE IF EXISTS `put database name here`;
CREATE DATABASE IF NOT EXISTS `put database name here`;
